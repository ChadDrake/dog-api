import $ from 'jquery';

function getNumberOfDoggos(){
  let number = $('.number').val()
$('.number').val('');
  console.log(number);
};

function handleSumbit() {
  $('main').on('submit'){
getNumberOfDoggos
  }
};

function main() {
  
}

$(main);